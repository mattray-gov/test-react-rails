<%= form_with(model: banner) do |form| %>
  <% if banner.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(banner.errors.count, "error") %> prohibited this banner from being saved:</h2>

      <ul>
        <% banner.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :message, style: "display: block" %>
    <%= form.text_field :message %>
  </div>

  <div>
    <%= form.label :isOn, style: "display: block" %>
    <%= form.check_box :isOn %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
